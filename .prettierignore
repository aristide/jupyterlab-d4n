node_modules
**/node_modules
**/lib
**/package.json
!/package.json
jupyterlab_d4n

# Generated by packages/tokens/build.mjs. Committed for reviewability (PRD §7.2)
# but never hand-formatted — prettier would reformat them and the CI freshness
# check would then fail against a freshly-built copy.
packages/tokens/style/generated
packages/tokens/src/generated
packages/tokens/dist

# Imported verbatim from the Claude Design project. Keep it a faithful mirror:
# reformatting makes a re-import look like a diff.
design-reference

# The PRD and other scope documents are inputs, not source.
scope
